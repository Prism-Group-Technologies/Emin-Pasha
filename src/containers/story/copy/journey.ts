/** ⚠️ INVENTED MARKETING COPY — NOT YET CLIENT-APPROVED. See ./index.ts. */

/**
 * Chrome for the visual journey map. The stops themselves are the seven
 * `story.timeline` chapters, rendered by title verbatim — nothing here
 * restates or paraphrases the history, and no date or place is added. These
 * strings are only the rail's end-caps (both lifted verbatim from
 * `content/story.ts`) and the invitation to read on.
 */
export const journeyCopy = {
  startLabel: "Born in Germany",
  endLabel: "Killed at Kinene",
  note: "Each stop links straight to that chapter of the full account below.",
  stopCtaLabel: "Read chapter",
};
