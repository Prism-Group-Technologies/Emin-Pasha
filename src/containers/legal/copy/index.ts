/**
 * ⚠️ INVENTED LEGAL AND MARKETING COPY — NOT LEGAL ADVICE, NOT CLIENT-APPROVED.
 *
 * Nothing in this folder comes from docs/02_CONTENT_SOURCE_OF_TRUTH.md. It was
 * written to give the footer's legal routes real, readable pages: a layered
 * summary, a full document with a table of contents, and conversion bands.
 * The documents are drafted against Uganda's Data Protection and Privacy Act
 * 2019 (and its 2021 Regulations), the GDPR / UK GDPR for guests from Europe,
 * and WCAG 2.2 AA — but every retention period, cancellation window, cookie
 * name, on-site accessibility feature and response time is a **placeholder to
 * confirm**, and anything in square brackets (e.g. `[Registered company name]`)
 * must be filled in from company records.
 *
 * It lives here — colocated with the container, outside `src/content` — on
 * purpose, like `containers/faq/copy`, so `check:content` cannot mistake it
 * for approved copy.
 *
 * TODO(EMIN-Q40): a Ugandan data-protection lawyer must review and replace
 * these documents before launch, and the hotel must register with the
 * Personal Data Protection Office if it has not already.
 */

export { legalSections } from "./sections";
export { LEGAL_ENTITY } from "./entity";
export { privacyDocument } from "./privacy";
export { cookieDocument } from "./cookies";
export { termsDocument } from "./terms";
export { accessibilityDocument } from "./accessibility";
export { cookieSettingsCopy } from "./cookieSettings";
export { legalPromiseAsset } from "./media";
